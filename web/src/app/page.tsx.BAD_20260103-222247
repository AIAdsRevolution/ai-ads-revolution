import Link from "next/link";

export default function HomePage() {
  return (
    <main className="min-h-screen bg-[#0B0F1A] text-white">
      {/* TOP GLOW */}
      <div className="pointer-events-none fixed inset-0 -z-10">
        <div className="absolute -top-40 left-1/2 h-[520px] w-[820px] -translate-x-1/2 rounded-full blur-3xl opacity-40"
             style={{ background: "radial-gradient(closest-side, rgba(45,108,255,.55), transparent 70%)" }} />
        <div className="absolute -top-20 left-[12%] h-[380px] w-[380px] rounded-full blur-3xl opacity-30"
             style={{ background: "radial-gradient(closest-side, rgba(123,97,255,.55), transparent 70%)" }} />
        <div className="absolute bottom-[-180px] right-[10%] h-[520px] w-[520px] rounded-full blur-3xl opacity-25"
             style={{ background: "radial-gradient(closest-side, rgba(62,243,255,.35), transparent 70%)" }} />
      </div>

      {/* HEADER */}
      <header className="ensuresafe px-6 pt-6">
        <div className="mx-auto flex max-w-6xl items-center justify-between rounded-2xl border border-white/10 bg-white/[0.04] px-4 py-3 backdrop-blur-xl">
          <div className="flex items-center gap-3">
            {/* LOGO placeholder (lo sostituiamo dopo con il logo vero) */}
            <div
              className="grid h-11 w-11 place-items-center rounded-xl border border-white/10"
              style={{ background: "linear-gradient(135deg, rgba(45,108,255,.9), rgba(123,97,255,.9))" }}
            >
              <span className="font-extrabold tracking-wide">AI</span>
            </div>
            <div className="leading-tight">
              <div className="font-semibold">AI Ads Revolution</div>
              <div className="text-xs text-white/60">Motore neurale • decision log • realtime</div>
            </div>
          </div>

          <nav className="hidden items-center gap-2 md:flex">
            <a className="rounded-full px-3 py-2 text-sm text-white/70 hover:bg-white/5 hover:text-white" href="#come-funziona">Come funziona</a>
            <a className="rounded-full px-3 py-2 text-sm text-white/70 hover:bg-white/5 hover:text-white" href="#ai-engine">AI Engine</a>
            <a className="rounded-full px-3 py-2 text-sm text-white/70 hover:bg-white/5 hover:text-white" href="#prezzi">Prezzi</a>
            <a className="rounded-full px-3 py-2 text-sm text-white/70 hover:bg-white/5 hover:text-white" href="#status">Status</a>
          </nav>

          <div className="flex items-center gap-2">
            <Link
              href="/dashboard"
              className="rounded-full border border-white/10 bg-white/[0.06] px-4 py-2 text-sm font-semibold hover:bg-white/[0.10]"
            >
              Accedi
            </Link>
            <Link
              href="/dashboard"
              className="rounded-full px-4 py-2 text-sm font-semibold text-white"
              style={{ background: "linear-gradient(135deg, #2D6CFF, #7B61FF)" }}
            >
              Registrati
            </Link>
          </div>
        </div>
      </header>

      {/* HERO */}
      <section className="px-6 pb-10 pt-10 md:pt-14">
        <div className="mx-auto grid max-w-6xl grid-cols-1 gap-8 md:grid-cols-2">
          {/* LEFT */}
          <div>
            <div className="inline-flex flex-wrap items-center gap-2 rounded-full border border-white/10 bg-white/[0.04] px-3 py-2 text-xs text-white/70">
              <span className="rounded-full border border-white/10 bg-white/[0.06] px-2 py-1">AI Neural Campaign Engine</span>
              <span className="rounded-full border border-white/10 bg-white/[0.06] px-2 py-1">Realtime signals</span>
              <span className="rounded-full border border-white/10 bg-white/[0.06] px-2 py-1">Meta-like UI</span>
              <span className="rounded-full border border-white/10 bg-white/[0.06] px-2 py-1">Audit-ready</span>
            </div>

            <h1 className="mt-6 text-4xl font-extrabold leading-tight tracking-tight md:text-6xl">
              Crea campagne che{" "}
              <span className="bg-gradient-to-r from-[#2D6CFF] via-[#7B61FF] to-[#3EF3FF] bg-clip-text text-transparent">
                migliorano da sole
              </span>
              .
            </h1>

            <p className="mt-5 max-w-xl text-base text-white/70 md:text-lg">
              Un motore neurale che rialloca budget, ottimizza creatività e bid in tempo reale.
              Decision log trasparente e KPI misurabili. Progettato per crescere da startup a enterprise.
            </p>

            <div className="mt-6 flex flex-wrap items-center gap-3">
              <Link
                href="/dashboard"
                className="rounded-full px-5 py-3 text-sm font-semibold text-white shadow-[0_18px_60px_rgba(45,108,255,.25)]"
                style={{ background: "linear-gradient(135deg, #2D6CFF, #7B61FF)" }}
              >
                Inizia ora
              </Link>
              <a
                href="#come-funziona"
                className="rounded-full border border-white/10 bg-white/[0.04] px-5 py-3 text-sm font-semibold text-white/90 hover:bg-white/[0.08]"
              >
                Guarda come funziona
              </a>

              <div className="ml-1 text-xs text-white/55">
                Prova gratuita • Nessun lock-in • Setup veloce
              </div>
            </div>

            {/* KPI STRIP */}
            <div className="mt-8 grid grid-cols-2 gap-3 md:grid-cols-4">
              <KpiCard label="Vendite medie attribuite (beta)" value="+30%" />
              <KpiCard label="ROAS medio (beta)" value="4.7x" accent />
              <KpiCard label="Riduzione CPC media" value="−25%" />
              <KpiCard label="Setup iniziale" value="< 5 min" />
            </div>

            <div className="mt-3 text-xs text-white/45">
              *Valori indicativi su inserzionisti beta. I risultati medi non garantiscono performance future.
            </div>
          </div>

          {/* RIGHT - PREVIEW */}
          <div className="relative">
            <div className="absolute -inset-2 -z-10 rounded-3xl blur-2xl opacity-40"
                 style={{ background: "radial-gradient(closest-side, rgba(45,108,255,.55), transparent 60%)" }} />

            <div className="rounded-3xl border border-white/10 bg-white/[0.04] p-4 backdrop-blur-xl">
              <div className="flex items-center justify-between">
                <div>
                  <div className="text-sm font-semibold">Performance preview</div>
                  <div className="text-xs text-white/55">AI Neural Campaign Engine • ultimi 28 giorni</div>
                </div>
                <div className="inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/[0.06] px-3 py-2 text-xs">
                  <span className="h-2 w-2 rounded-full bg-emerald-400" />
                  <span className="text-white/80">AI ON</span>
                </div>
              </div>

              <div className="mt-4 rounded-2xl border border-white/10 bg-[#0B0F1A]/60 p-4">
                <div className="text-xs text-white/55">Trend AI</div>
                <div className="mt-3 h-36 rounded-2xl border border-white/10"
                     style={{ background: "linear-gradient(135deg, rgba(45,108,255,.20), rgba(123,97,255,.18))" }} />
                <div className="mt-4 grid grid-cols-2 gap-3">
                  <MiniCard title="CTR medio" value="0.0%" sub="Click / impression" />
                  <MiniCard title="CPC medio" value="€ 0.00" sub="Costo per clic" />
                  <MiniCard title="ROAS medio" value="0.0x" sub="Ritorno spesa adv" />
                  <MiniCard title="AI Actions" value="0" sub="Ottimizzazioni applicate" />
                </div>

                <div className="mt-4 text-xs text-white/55">
                  Collega campagne reali per vedere KPI live. La dashboard è l’area dei dati dettagliati.
                </div>

                <div className="mt-3 flex flex-wrap gap-2">
                  <span className="rounded-full border border-white/10 bg-white/[0.04] px-3 py-2 text-xs text-white/70">Decision log</span>
                  <span className="rounded-full border border-white/10 bg-white/[0.04] px-3 py-2 text-xs text-white/70">Budget shift</span>
                  <span className="rounded-full border border-white/10 bg-white/[0.04] px-3 py-2 text-xs text-white/70">Creative AI</span>
                </div>
              </div>
            </div>

            {/* FLOATING NOTE (neural vibe) */}
            <div className="mt-4 rounded-2xl border border-white/10 bg-white/[0.04] p-4 text-xs text-white/65 backdrop-blur-xl">
              <div className="font-semibold text-white/80">AI-first in ogni decisione</div>
              <div className="mt-2">Ogni asta, offerta e creatività viene valutata da segnali realtime e log trasparente.</div>
            </div>
          </div>
        </div>
      </section>

      {/* FEATURES */}
      <section id="come-funziona" className="px-6 pb-12">
        <div className="mx-auto max-w-6xl">
          <div className="grid grid-cols-1 gap-4 md:grid-cols-3">
            <FeatureCard title="AI-first in ogni decisione" text="Ogni asta, offerta e creatività viene valutata da segnali realtime." />
            <FeatureCard title="Decision log trasparente" text="Sai cosa fa l’AI, perché lo fa e con quale impatto stimato." />
            <FeatureCard title="Realtime signals" text="Reagisce ai cambiamenti del mercato mentre accadono." />
            <FeatureCard title="Scalabilità enterprise" text="Inizi piccolo e cresci solo quando i dati lo confermano." />
            <FeatureCard title="Setup guidato" text="La guida AI integrata ti mostra step-by-step come ottimizzare." />
            <FeatureCard title="Integrazioni" text="Supabase + Google Ads: KPI reali, report, automazioni." />
          </div>

          {/* CTA BAR */}
          <div className="mt-10 flex flex-col items-start justify-between gap-4 rounded-3xl border border-white/10 bg-white/[0.04] p-6 backdrop-blur-xl md:flex-row md:items-center">
            <div>
              <div className="text-xl font-bold">Inizia oggi con AI Ads Revolution</div>
              <div className="mt-1 text-sm text-white/65">Setup rapido, prova gratuita e controllo totale sulle ottimizzazioni AI.</div>
            </div>
            <div className="flex items-center gap-3">
              <Link
                href="/dashboard"
                className="rounded-full px-5 py-3 text-sm font-semibold text-white"
                style={{ background: "linear-gradient(135deg, #2D6CFF, #7B61FF)" }}
              >
                Crea un account
              </Link>
              <Link
                href="/dashboard"
                className="rounded-full border border-white/10 bg-white/[0.04] px-5 py-3 text-sm font-semibold text-white/90 hover:bg-white/[0.08]"
              >
                Accedi alla dashboard
              </Link>
            </div>
          </div>

          <footer className="mt-10 flex flex-col items-start justify-between gap-4 border-t border-white/10 pt-6 text-xs text-white/50 md:flex-row md:items-center">
            <div>© {new Date().getFullYear()} AI Ads Revolution. Tutti i diritti riservati.</div>
            <div className="flex gap-4">
              <a className="hover:text-white/80" href="#status">Status</a>
              <a className="hover:text-white/80" href="#prezzi">Prezzi</a>
              <a className="hover:text-white/80" href="#ai-engine">AI Engine</a>
            </div>
          </footer>
        </div>
      </section>
    </main>
  );
}

function KpiCard({ label, value, accent }: { label: string; value: string; accent?: boolean }) {
  return (
    <div className="rounded-2xl border border-white/10 bg-white/[0.04] p-4 backdrop-blur-xl">
      <div className="text-[11px] text-white/55">{label}</div>
      <div className={"mt-2 text-2xl font-extrabold " + (accent ? "text-[#3EF3FF]" : "text-white")}>
        {value}
      </div>
    </div>
  );
}

function MiniCard({ title, value, sub }: { title: string; value: string; sub: string }) {
  return (
    <div className="rounded-2xl border border-white/10 bg-white/[0.04] p-3">
      <div className="text-[11px] text-white/55">{title}</div>
      <div className="mt-1 text-lg font-bold">{value}</div>
      <div className="text-[11px] text-white/45">{sub}</div>
    </div>
  );
}

function FeatureCard({ title, text }: { title: string; text: string }) {
  return (
    <div className="rounded-3xl border border-white/10 bg-white/[0.04] p-6 backdrop-blur-xl">
      <div className="text-base font-bold">{title}</div>
      <div className="mt-2 text-sm text-white/65">{text}</div>
    </div>
  );
}
