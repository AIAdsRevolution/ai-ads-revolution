"use client";

export default function Topbar() {
  function togglePro() {
    window.dispatchEvent(new CustomEvent("proassistant:toggle"));
  }

  return (
    <header className="h-14 border-b border-white/10 bg-slate-950/60 backdrop-blur-xl">
      <div className="mx-auto flex h-full max-w-[1200px] items-center justify-between px-4">
        <div className="flex items-center gap-3">
          <div className="text-sm font-semibold text-white/90">
            AI Ads Revolution
          </div>
          <div className="text-xs text-white/50">Dashboard</div>
        </div>

        <div className="flex items-center gap-2">
          {/* UNICO entrypoint - PRO Assistant */}
          <button
            onClick={togglePro}
            className="group relative inline-flex items-center gap-2 rounded-full border border-sky-300/20 bg-sky-500/10 px-4 py-2 text-sm font-semibold text-white shadow-2xl backdrop-blur transition hover:bg-sky-500/15 active:scale-[0.98]"
            style={{
              boxShadow:
                "0 0 0 1px rgba(56,189,248,0.14), 0 18px 50px rgba(0,0,0,0.55), 0 0 44px rgba(56,189,248,0.12)",
            }}
            aria-label="Apri PRO Assistant"
          >
            <span
              className="inline-flex h-8 w-8 items-center justify-center rounded-full bg-white/10"
              style={{ boxShadow: "0 0 18px rgba(56,189,248,0.22)" }}
            >
              AI
            </span>
            <span className="flex flex-col items-start leading-tight">
              <span className="text-[13px]">PRO Assistant</span>
              <span className="text-[10px] opacity-70">Neural Sky</span>
            </span>

            <span className="ml-1 rounded-full bg-emerald-400/15 px-2 py-1 text-[10px] font-semibold text-emerald-200 ring-1 ring-emerald-400/20">
              PRO
            </span>
          </button>
        </div>
      </div>
    </header>
  );
}
